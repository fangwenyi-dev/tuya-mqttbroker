## window_broker ESP32-S3 板级配置
## 通过 tos.py config 选择 ESP32 平台、esp32s3 芯片后，本文件提供默认配置。
##
## 关键配置说明：
## - ENABLE_WIFI=1: 启用 WiFi（涂鸦云通信 + MQTT Broker）
## - ENABLE_BT_SERVICE=y: 启用涂鸦 BT 服务（BLE 配网 + 蓝牙控制）
## - ENABLE_BT_NETCFG=y: 启用 BLE+AP 配网（涂鸦 App 原生配网方式）
## - ENABLE_NIMBLE=y: 使用 NimBLE 协议栈（内存占用更小）
## - ENABLE_LIBLWIP=1: 启用 lwIP 协议栈（MQTT/WiFi 需要）
##
## 相比 Matter 方案的变化：
## - 移除 ESP_MATTER 相关配置
## - 移除 ESP-IDF 组件依赖（改为 TuyaOpen SDK 管理）
## - 增加 BLE 配网配置（NETCFG_TUYA_BLE）

## WiFi 配置
CONFIG_ENABLE_WIFI=1

## BLE 配网配置
CONFIG_ENABLE_BT_SERVICE=y
CONFIG_ENABLE_BT_NETCFG=y
CONFIG_ENABLE_NIMBLE=y

## lwIP 协议栈（MQTT/WiFi 必需）
CONFIG_ENABLE_LIBLWIP=1

## KV 存储（涂鸦授权信息、设备状态）
CONFIG_ENABLE_KV=1

## OTA 升级
CONFIG_ENABLE_OTA=y

## 日志级别（开发阶段使用 DEBUG，生产环境改为 NOTICE）
CONFIG_LOG_LEVEL=4

## mDNS（局域网设备发现，涂鸦 LAN 控制需要）
CONFIG_ENABLE_MDNS=y

## 串口日志输出
CONFIG_ENABLE_TKL_OUTPUT=y
